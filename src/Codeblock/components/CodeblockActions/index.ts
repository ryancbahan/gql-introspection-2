/**
 * Implements the Actions component that host a list of actions
 *
 * Components:
 * - actions container
 * - action component
 *    - Allows custom content (e.g. icons) and action callback.
 *    - Collapses with limited real estate.
 *    - Tooltip
 *    - Example actions:
 *      - links (npm, github, etc.)
 *      - copy
 *      - run (maybe post M1)
 */

export {CodeblockActions} from './CodeblockActions';
